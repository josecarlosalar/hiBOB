import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Tests de widgets se implementarán en fases posteriores
    expect(true, isTrue);
  });
}
